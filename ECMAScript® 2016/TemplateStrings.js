/**
 * DECLARAÇÃO DE UMA VARIÁVEL NOME, CONTENDO O TEXTO WESLEY FLÔRES
 **/
let nome = "Wesley Flôres"

console.log(`Olá, ${nome}`);

const nome = 'Rebeca'
const concatenacao = 'Olá ' + nome + '!'
const template = `
    Olá
    ${nome}!`
console.log(concatenacao, template)

// expressoes...
console.log(`1 + 1 = ${1 + 1}`)

const up = texto => texto.toUpperCase()
console.log(`Ei... ${up('cuidado')}!`)